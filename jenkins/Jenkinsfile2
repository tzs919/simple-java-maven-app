pipeline {
    agent any
    stages {

        stage('Init sona') {
            steps {
                withSonarQubeEnv() {
                  bat "mvn sonar:sonar"
                }
            }
        }

    }
}